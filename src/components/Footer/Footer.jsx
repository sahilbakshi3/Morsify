import React from 'react';

const Footer = () => {
  return (
    <div className="text-center text-gray-400 mt-8">
      <p className="text-sm">
        Built with React, Tailwind CSS, and integrated with translation APIs
      </p>
      <p className="text-xs mt-2">
        Supports both online API translation and offline backup translation
      </p>
    </div>
  );
};

export default Footer;